- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zsh:
    ~/.vimrc:
    ~/.bashrc:
    ~/.zshrc:
    ~/.fzf.bash:
    ~/.fzf.zsh:
    ~/.gitconfig:
    ~/.npmrc:
    ~/.yarnrc:
    ~/.quadrarc:


- create:
    - ~/Work

- shell:
  - command: ./setup_homebrew.zsh
