- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zsh:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.npmrc:


- create:
    - ~/Work
    - ~/.nvm

- shell:
  - command: ./setup_homebrew.zsh
    description: Install Homebrew packages and applications
