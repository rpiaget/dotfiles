- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.bashrc:
    ~/.docker:
    ~/.dockercfg:
    ~/.env:
    ~/.fzf.bash:
    ~/.fzf.zsh:
    ~/.gitconfig:
    ~/.npmrc:
    ~/.sl:
    ~/.tldrc:
    ~/.yarnrc:


- create:
    - ~/Work

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
