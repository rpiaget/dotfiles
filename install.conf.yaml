- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.bashrc:
    ~/.env:
    ~/.fzf.bash:
    ~/.fzf.zsh:
    ~/.gitconfig:
    ~/.git-completion.bash:
    ~/.npmrc:
    ~/.yarnrc:
    ~/.quadrarc:


- create:
    - ~/Work

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
