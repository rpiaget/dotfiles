- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:

- create:
    - ~/Work

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"