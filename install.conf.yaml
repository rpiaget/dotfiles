- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.bashrc:
    ~/.docker:
    ~/.dockercfg:
    ~/.env:
    ~/.fzf.bash:
    ~/.fzf.zsh:
    ~/.gitconfig:
    ~/.npm:
    ~/.npmrc:
    ~/.nvm:
    ~/.sl:
    ~/.tldrc:
    ~/.yarn:
    ~/.yarnrc:




- create:
    - ~/Work

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
